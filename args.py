"""
    Write a program which takes the first two command-line arguments
    and add them together (as numbers), then print out the sum multiplied by
    each of the elements in the list: [1, 2, 3, 4].

    e.g.
    $ python3 args.py 4 5
    9
    18
    27
    36
"""

import sys
print(sys.)
